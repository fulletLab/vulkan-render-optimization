# Examples

Example projects and scenes live here as relevant engine systems land.

- `basic_scene` covers the Scene/ECS JSON roundtrip example.
- `basic_assets` provides a textured glTF import fixture for the Phase 6 Project Browser flow.

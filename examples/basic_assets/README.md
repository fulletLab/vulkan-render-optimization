# Basic Assets Example

`TexturedTriangle.gltf` is a tiny glTF 2.0 model with embedded vertex data and an embedded PNG base-color texture. Import it from the Project Browser during Phase 6 to create a scene entity backed by the asset pipeline.

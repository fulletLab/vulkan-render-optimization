# Basic Scene Example

`BasicScene.scene.json` is the minimal Phase 2/3/4 scene example. It contains a parent entity and a child entity with transforms, can be loaded through the editor's File > Open Scene command, is visible/selectable in the Scene View viewport, and can be transformed with the Move/Rotate/Scale gizmos.
